// Add the dialog title
$('#dialog_teacher h3').html("New Teacher");

// Render the new form
$('.modal-body').html('<%= j render("form") %>');

// Show the dynamic dialog
$('#dialog_teacher').modal("show");

// Set focus to the first element
$('#dialog_teacher').on('shown.bs.modal', function () {
	  $('.first_input').focus()
  })