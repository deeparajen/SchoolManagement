
    <div class="row">
			<div class="col-md-6">
				<div class="panel panel-primary">
					<div class="panel-heading">
						<h3 class="panel-title">Listing Subjects</h3>
						<div class="pull-right" style= "text-align: center;line-height: 1px;">
							<%= link_to 'Add Subject', new_subject_path, {:style => "color: inherit;text-align: center"}%>
						</div>
					</div>
					
					<table class="table table-bordered" id="dev-table">
						<thead>
							<tr>
								<th>#</th>
								<th>Subject Name</th>
								<th colspan="2" style="text-align: center">Action</th>
								
							</tr>
						</thead>
						<tbody>
							<%i =0%>
							<% @subjects.each do |subject| %>
								<tr>
								<td><%= i+ 1%></td>
								<td> <%= subject.subject_name%>	</td>
								<td><%= link_to 'Edit', edit_subject_path(subject) %></td>
								<td><%= link_to 'Destroy', subject_path(subject),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
								</tr>
								<%i=i+1%>
							<%end%>
							
						</tbody>
					</table>
				</div>
			</div>
			
		</div>
